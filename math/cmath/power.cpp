#include <iostream>
// with powers, you have to include the cmath headerfile.
#include <cmath>

int main(){
   double x = 4;
   double y = 2;
   double z;

   z = std::pow(2, 4); // you can also use variables in place of the numbers

   std::cout << z;

   return 0;
}