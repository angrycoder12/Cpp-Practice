#include <iostream>
// you have to add the cmath headerfile
#include <cmath>

int main(){
   double z;

   z = std::sqrt(16); // you can also use variables in place of 16
   std::cout << "the square root of 16 is " << z;

    return 0;
}